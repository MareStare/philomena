# VSCode supports `jsonc` for all of its JSON files.
# For some reason `.vscode/*.json` doesn't work. We have to specify the file name exactly.
.vscode/extensions.json linguist-language=jsonc

# We use PostCSS for CSS files
*.css linguist-language=PostCSS
